import { ConfigGlobalDirective } from './config-global.directive';

describe('ConfigGlobalDirective', () => {
  it('should create an instance', () => {
    const directive = new ConfigGlobalDirective();
    expect(directive).toBeTruthy();
  });
});
